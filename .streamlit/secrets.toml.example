# .streamlit/secrets.toml (local development only)
# This file is gitignored and should NOT be committed.
# Create this file locally with your configuration.

# Database path (local dev)
db_path = "tenders.db"

# Optional: Add API keys or sensitive config here
# You can provide data.gov.in API key in one of two ways:
# Option A (recommended):
# [data_gov]
# api_key = "your-data-gov-api-key"
# Option B (flat):
# data_gov_api_key = "your-data-gov-api-key"
